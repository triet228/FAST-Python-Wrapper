{
  "title": "FAST Wrapper Output Schema",
  "description": "Schema for the dictionary returned by FastWrapper.run().",
  "type": "object",
  "required": [
    "status",
    "log",
    "output"
  ],
  "additionalProperties": false,
  "properties": {
    "status": {
      "type": "string",
      "description": "Whether FAST produced an OutputAircraft dictionary.",
      "enum": [
        "Yes",
        "No"
      ]
    },
    "log": {
      "type": "string",
      "description": "MATLAB stdout captured during the FAST run."
    },
    "output": {
      "description": "FAST OutputAircraft dictionary. Empty when FAST does not produce OutputAircraft.",
      "anyOf": [
        {
          "$ref": "OutputAircraftSchema.json"
        },
        {
          "type": "object",
          "maxProperties": 0
        }
      ]
    }
  }
}
